/**
 * @hushbid/sdk/testing
 *
 * Test-only utilities. NOT part of the public SDK surface.
 * These are exported separately so they don't pollute the main import.
 *
 * @example
 * ```ts
 * import { MOCK_NFT_ABI } from "@hushbid/sdk/testing";
 * ```
 */

export { MOCK_NFT_ABI } from "./client";
