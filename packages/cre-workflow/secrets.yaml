# ==========================================================================
# CRE SECRETS FILE
# ==========================================================================
# Secrets are stored in the VaultDON and injected at runtime.
# Values here are placeholders — set real values via `cre secrets set`.
#
# Namespace: hush-bid
# ==========================================================================

secretsNames:
  PINATA_JWT:
    - PINATA_JWT
  DON_ETH_PRIVATE_KEY:
    - DON_ETH_PRIVATE_KEY
  DON_WALLET_ADDRESS:
    - DON_WALLET_ADDRESS
  DON_ENCRYPTION_PRIVATE_KEY:
    - DON_ENCRYPTION_PRIVATE_KEY